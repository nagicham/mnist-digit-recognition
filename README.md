# MNIST Digit Recognition
A web service that recognizes handwritten digits using a CNN model trained on MNIST dataset.

## Overview
This project provides a web service that allows users to draw a digit in a web browser and classifies it as a number from 0 to 9.
The deep learning model is implemented based on "ゼロから作るDeep Learing".

